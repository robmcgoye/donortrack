
<%= turbo_frame_tag @user do %>
  <%# border border-primary %>
  <div class="container-fluid p-2 m-1">
    <div class="card">
      <%= render "edit_header", user: @user %>
      <div class="card-body"><%= render "form", user: @user %></div>
    </div>
  </div>
<% end %>

