<%# locals: (user:) -%>
<%=turbo_frame_tag "user_edit_header_#{user.id}" do %>
  <% if user.admin?%>
  <div class="card-header bg-primary text-white p-2 rounded"><h6><i class="bi bi-shield-lock"></i>Editing User</h6></div>
  <% else %>
  <div class="card-header"><h6>Editing User</h6></div>
  <% end %>
<% end %>